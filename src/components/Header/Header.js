import Navbar from "../Navbar/Navbar";

export default function Header() {
    return (
        <header>
            <h1 className="title">Your Breed Finder</h1>
            <Navbar />
        </header>
    );
}